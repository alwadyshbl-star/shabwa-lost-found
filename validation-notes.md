# ملاحظات التحقق قبل الإصدار

## المراجعة المرئية الموثقة

أُجريت المراجعة على سطح المكتب ضمن جلسة مشرف مسجلة الدخول، وباستخدام البلاغ الصالح رقم `1` المملوك للحساب نفسه. ظهر اتجاه RTL وتخطيط الشريط العلوي والتذييل بصورة سليمة في جميع المسارات التالية:

| المسار | نتيجة المراجعة |
| --- | --- |
| `/reports/1/edit` | ظهرت بيانات البلاغ داخل نموذج التعديل، مع زر الحفظ وبطاقة **رفع وتغيير الصور قيد التطوير** بصورة واضحة. |
| `/my-reports` | ظهرت الإحصاءات وبطاقة البلاغ وإجراءات العرض والتعديل والمطابقات والاسترجاع ضمن تخطيط متوازن. |
| `/reports/1` | ظهرت تفاصيل البلاغ وبطاقة بيانات التواصل وإجراء الإبلاغ عن تطابق بوضوح. |
| `/admin` | ظهرت بطاقات الإحصاءات وتبويبات إدارة البلاغات والمستخدمين ورسالة الإجراءات الحساسة بصورة سليمة. |
| `/notifications` | ظهرت حالة الإشعارات وبطاقة التطابق وإجراء مراجعة التطابق بشكل متماسك. |

## التحقق التفاعلي ورسائل الفشل

أُعيد تنفيذ طلبين مرفوضين بحساب مستخدم عادي عبر واجهات الخدمة التي تستدعيها الواجهة، ثم تم التحقق من تطابق الرسالة العربية المستلمة مع المعالجة الموحدة في الواجهة.

| العملية | النتيجة |
| --- | --- |
| الإبلاغ عن تطابق لبلاغ لا يخص المستخدم | `لا يمكنك الإبلاغ عن تطابق لهذا البلاغ.` |
| فتح بيانات مركز الإدارة بحساب غير مشرف | `هذه العملية متاحة للمشرف فقط.` |

تستخدم صفحات **تعديل البلاغ** و**لوحة المستخدم** و**تفاصيل البلاغ** و**الإدارة** و**الإشعارات** الدالة المشتركة `getFriendlyErrorMessage` مع تنبيه مرئي أو رسالة نموذج مناسبة، وقد أضيفت اختبارات تحول دون استبدال رسالتي الصلاحيات العربيتين برسائل تقنية عامة.

## اختبار متصفح تفاعلي مستقل

شُغّل اختبار متصفح مستقل ضمن جلسة اختبار حقيقية، ثم حُجبت استجابات الإجراءات عمدًا برسائل عربية ليُتحقق من المسار الذي يراه المستخدم، لا من طبقة الخدمة فقط. نجحت الحالات الخمس، وسُجلت لقطات لكل منها.

| الصفحة والإجراء | الرسالة الظاهرة داخل الواجهة | نتيجة المراجعة المرئية |
| --- | --- | --- |
| تعديل البلاغ | `تعذر تحديث البلاغ في اختبار الواجهة.` | ظهرت الرسالة كتنبيه أحمر أعلى الصفحة **وكخطأ داخل النموذج**، مع بقاء نموذج التعديل متاحًا للمحاولة مجددًا. |
| لوحة المستخدم — الاسترجاع | `تعذر استرجاع البلاغ في اختبار الواجهة.` | ظهر التنبيه الأحمر أعلى لوحة البلاغات دون تغيير حالة البلاغ. |
| تفاصيل البلاغ — الإبلاغ عن تطابق | `تعذر الإبلاغ عن التطابق في اختبار الواجهة.` | ظهر التنبيه الأحمر أعلى صفحة التفاصيل مع بقاء سياق البلاغ وإجراء المطابقة واضحين. |
| مركز الإدارة — تغيير حالة البلاغ | `تعذر تحديث حالة البلاغ في اختبار الواجهة.` | ظهر التنبيه الأحمر داخل مركز الإدارة مع استمرار عرض ملخص الإحصاءات والواجهة الإدارية. |
| الإشعارات — تعليم إشعار كمقروء | `تعذر تعليم الإشعار كمقروء في اختبار الواجهة.` | ظهر التنبيه الأحمر أعلى صفحة الإشعارات مع استمرار بطاقة الإشعار وإجراء المراجعة. |

## إعدادات الحساب

أُجريت مراجعة مستقلة ضمن جلسة مستخدم اختبار مسجلة الدخول على سطح المكتب والجوال. عرضت الصفحة الاسم والبريد الإلكتروني، ونموذج تغيير كلمة المرور، ومنطقة حذف الحساب النهائية بوضوح، مع بقاء الهوامش وأزرار الإجراءات قابلة للاستخدام على شاشة الجوال.

| التفاعل | النتيجة المتحققة داخل الواجهة |
| --- | --- |
| تأكيد كلمة المرور الجديدة غير المتطابق | ظهرت رسالة عربية داخل النموذج تطلب تطابق التأكيد مع كلمة المرور الجديدة. |
| كلمة المرور الحالية الخاطئة | ظهر تنبيه ورسالة نموذج: `كلمة المرور الحالية غير صحيحة.` |
| عبارة حذف الحساب غير الصحيحة | ظهرت رسالة تطلب كتابة `حذف حسابي` قبل تنفيذ العملية النهائية. |
| حذف الحساب الصحيح | حُذف حساب الاختبار، ومُسحت الجلسة، وعاد المتصفح إلى الصفحة الرئيسية. |
